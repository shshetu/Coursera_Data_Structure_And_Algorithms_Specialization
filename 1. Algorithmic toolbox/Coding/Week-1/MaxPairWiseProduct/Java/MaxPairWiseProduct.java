import java.util.Scanner;

public class MaxPairWiseProduct{
    public static void main(String[] args) {
        
    }
}

static class FastScanner(InputStream InputStream){
    // Create 2 objects for buffered reader and string tokenizer
    BufferedReader br;
    StringTokenizer stringTokenizer;
    // Using try catch make an object of buffered reader taking inputStream
    try {
        br = new BufferedReader(inputStream);
    } catch (Exception e) {
        e.printStackTrace();
    }
    // Using while loop using readLine method of buffered reader , create a method called next.
    String next(){
        while(stringTokenizer == null || !stringTokenizer.hasMoreTokens()){
            stringTokenizer
        }
    }
    //This method will return nextToken()
    // Create a method: nextInt() where we will parse the string to integer  
}